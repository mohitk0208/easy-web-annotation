import * as React from "react"
import * as ReactDOM from "react-dom"

import Settings from "./pages/Settings"

var mountPoint = document.getElementById("options");
ReactDOM.render( <Settings /> , mountPoint)